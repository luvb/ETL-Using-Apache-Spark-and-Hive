# ETL-Using-Apache-Spark-and-Hive
In this example, we'll read a sample data set with Spark on HDFS (Hadoop File System), do a simple analytical operation, then write to a table that we'll make in Hive.
